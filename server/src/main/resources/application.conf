ref {
  db {
    enabled = ${?DB_ENABLED}
    enabled = false
    url = "jdbc:mysql://localhost:3306/refapp?useSSL=false"
    url = ${?DB_URL}
    user = "refapp"
    user = ${?DB_USER}
    pass = "changeme"
    pass = ${?DB_PASS}
  }
}
